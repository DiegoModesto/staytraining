# ---------- .NET ----------
[Bb]in/
[Oo]bj/
*.user
*.suo
.vs/
.nuget-cache/
.containers/
TestResults/
coverage.cobertura.xml
*.trx

# ---------- Flutter / Dart ----------
mobile/.dart_tool/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
mobile/build/
mobile/.pub-cache/
mobile/.pub/
mobile/pubspec.lock
# Native platform folders are versioned (they carry OIDC redirect scheme, HTTP/ATS dev config,
# and future push/Firebase setup). Build artifacts are excluded by mobile/android/.gitignore and
# mobile/ios/.gitignore. Desktop/web targets are unused — keep regenerating them with `flutter create .`.
mobile/linux/
mobile/macos/
mobile/windows/
mobile/web/

# ---------- Secrets / certs ----------
**/dev-certs/
*.pfx

# ---------- OS / editor ----------
.DS_Store
Thumbs.db
*.swp

# ---------- Agent / local ----------
.claude/
